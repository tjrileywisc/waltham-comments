[project]
name = "embeddings-service"
version = "0.1.0"
description = "HTTP service for generating sentence embeddings"
requires-python = ">=3.13"
dependencies = [
    "sentence-transformers>=5.2.2",
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.20.0",
    "numpy>=1.24.0",
]
